import { useState } from "react";

function Header() {
    // const[name, setName] = useState( null );
    return(
        <div className="App-header">
            <h1>Jen's Calculator</h1>
        </div>
    )
}

export default Header;